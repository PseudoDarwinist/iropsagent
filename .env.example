# Email Configuration for Flight Disruption Notifications
# Copy this file to .env and fill in your email settings

# SMTP Server Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Email Credentials
# For Gmail, use App Password instead of regular password
# Instructions: https://support.google.com/accounts/answer/185833
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Sender Email (defaults to SMTP_USERNAME if not set)
FROM_EMAIL=travel-alerts@yourcompany.com

# Database Configuration (optional, defaults to SQLite)
DATABASE_URL=sqlite:///./travel_disruption.db

# Example for PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost:5432/travel_disruption